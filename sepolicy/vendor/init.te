# Block device
allow init bootloader_block_device:lnk_file relabelto;

# Mount
allow init firmware_mnt_file:dir mounton;
allow init firmware_mnt_file:filesystem { mount unmount relabelfrom relabelto };
