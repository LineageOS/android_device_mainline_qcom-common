type vendor_pil_squasher, domain;
type vendor_pil_squasher_exec, exec_type, vendor_file_type, file_type;

allow vendor_pil_squasher tmpfs:dir rw_dir_perms;
allow vendor_pil_squasher tmpfs:file create_file_perms;

r_dir_file(vendor_pil_squasher, firmware_mnt_file)
